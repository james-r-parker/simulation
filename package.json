{
  "name": "blob-evolution-simulation",
  "version": "5.0.0",
  "description": "Artificial life simulation using neuroevolution with WebGL rendering",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "serve": "npx http-server -p 8000 -c-1",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist",
    "deploy:preview": "npm run build && wrangler pages deployment create --env preview"
  },
  "keywords": [
    "simulation",
    "evolution",
    "neural-network",
    "webgl",
    "three.js"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "vite": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}



